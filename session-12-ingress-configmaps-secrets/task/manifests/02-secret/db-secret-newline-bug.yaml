# Task 4 demo: the SAME password encoded the WRONG way, with `echo` (not `echo -n`),
# so the base64 payload carries an invisible trailing 0x0A newline byte.
apiVersion: v1
kind: Secret
metadata:
  name: yatri-db-secret-bad
  namespace: s12-lab
  labels:
    app: yatri-app
type: Opaque
data:
  POSTGRES_PASSWORD: c2VjcmV0cGFzc3dvcmQK
