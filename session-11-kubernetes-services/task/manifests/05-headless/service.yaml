apiVersion: v1
kind: Service
metadata:
  name: web-service-headless
  namespace: s11-lab
spec:
  clusterIP: None                        # <-- HEADLESS: no VIP, no kube-proxy rules
  selector:
    app: web-headless
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
