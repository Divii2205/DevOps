apiVersion: v1
kind: Service
metadata:
  name: app-rolling-service
  namespace: s10-lab
  labels:
    app: app-rolling
spec:
  type: NodePort
  selector:
    app: app-rolling
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30110
      protocol: TCP
