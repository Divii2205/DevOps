apiVersion: v1
kind: Service
metadata:
  name: app-recreate-service
  namespace: s10-lab
  labels:
    app: app-recreate
spec:
  type: NodePort
  selector:
    app: app-recreate
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30140
      protocol: TCP
