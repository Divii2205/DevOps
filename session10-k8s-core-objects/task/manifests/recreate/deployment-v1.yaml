apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-recreate
  namespace: s10-lab
  labels:
    app: app-recreate
spec:
  replicas: 3
  # Recreate: the controller scales the OLD ReplicaSet to 0 and WAITS for every
  # old pod to disappear before it creates a single new pod. No two versions ever
  # run at once -- and there is a guaranteed outage window with zero endpoints.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: app-recreate
  template:
    metadata:
      labels:
        app: app-recreate
        version: v1
    spec:
      containers:
        - name: web
          image: nginx:1.24-alpine
          ports:
            - containerPort: 80
          command:
            - /bin/sh
            - -c
            - >
              echo '<html><body style="background:#1b262c;color:#3282b8;font-family:monospace;font-size:2.5em;text-align:center;padding-top:20vh"><p>STRATEGY: RECREATE</p><p>VERSION: v1</p><p style="font-size:0.4em">All v1 pods will be killed before v2 starts</p></body></html>'
              > /usr/share/nginx/html/index.html && exec nginx -g 'daemon off;'
          resources:
            requests: { cpu: "30m", memory: "32Mi" }
            limits:   { cpu: "100m", memory: "64Mi" }
          readinessProbe:
            httpGet: { path: /, port: 80 }
            initialDelaySeconds: 3
            periodSeconds: 5
